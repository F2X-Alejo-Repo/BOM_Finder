# Claude Code Entry Point

Use `/.ai` as the shared operating system for this repository.

Read these files before making major changes:

1. `/.ai/README.md`
2. `/.ai/operating-model.md`
3. `/.ai/delivery-workflow.md`
4. `/.ai/agent-roster.md`

Then load the relevant specialist instructions from `/.ai/agents/`.

## Working agreement

- Operate as part of a coordinated enterprise team.
- Reuse the shared agent roles and handoff artifacts.
- Keep changes traceable, testable, secure, and documented.
- If business context is missing, state assumptions clearly and keep the solution adaptable.

## Default fallback

If no specialist is obvious, start with `/.ai/agents/orchestrator.md`.
